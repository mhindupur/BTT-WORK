PORT=4000
DATABASE_URL=mysql://btt:btt@127.0.0.1:3307/btt_fleet
JWT_SECRET=change-me-to-long-random-string
# Browser CORS: fleet-web origin(s). Comma-separated if multiple. No trailing slash.
PUBLIC_WEB_ORIGIN=http://localhost:5174

# Owner payment links in WhatsApp: {PAYMENT_PUBLIC_BASE_URL}/pay/{token}
# If unset, same as PUBLIC_WEB_ORIGIN. Local: http://localhost:5174
# AWS / production: https://your-domain-or-alb.example.com
# PAYMENT_PUBLIC_BASE_URL=http://localhost:5174

UPLOAD_DIR=./uploads

# WhatsApp for payment links: stub | interakt | meta
WHATSAPP_PROVIDER=stub

# Interakt — template must have one {{1}} body variable = payment URL
# Use INTERAKT_API_KEY (Basic built in code) OR INTERAKT_AUTHORIZATION=Basic … from Interakt
# INTERAKT_API_KEY=
# INTERAKT_AUTHORIZATION=
# INTERAKT_TEMPLATE_NAME=test_invoice_template
# INTERAKT_TEMPLATE_LANG=en
# INTERAKT_API_URL=https://api.interakt.ai/v1/public/message/
# INTERAKT_AUTH_MODE=basic
# Or set full header: INTERAKT_AUTHORIZATION=Basic xxx

# Interakt — Authentication OTP template (must be approved as Authentication category)
# Template must have one body variable {{1}} = OTP
# INTERAKT_AUTH_OTP_TEMPLATE_NAME=your_auth_otp_template

# OTP TTL in minutes (default 10)
# AUTH_OTP_TTL_MINUTES=10

# Meta WhatsApp Cloud API — template with one body parameter (text = URL)
# META_WHATSAPP_PHONE_NUMBER_ID=
# META_WHATSAPP_ACCESS_TOKEN=
# META_WHATSAPP_TEMPLATE_NAME=
# META_WHATSAPP_TEMPLATE_LANG=en
# META_GRAPH_API_VERSION=v21.0

# Meta — Authentication OTP template ({{1}} = OTP)
# META_AUTH_OTP_TEMPLATE_NAME=

# Default admin (proposal: full system control)
ADMIN_EMAIL=admin@btt.fleet
ADMIN_PASSWORD=Admin@123

# Default supervisor / site manager (proposal: field operations)
DEMO_SM_EMAIL=supervisor@btt.fleet
DEMO_SM_PASSWORD=Supervisor@123
